# Google Antigravity — Autonomous Futures Bot completion mandate

## Assignment

Take over as the sole coding writer and deliver this project end to end. Do not stop at a plan, provider smoke, disconnected modules, paper analytics, or a sequence of tiny phases. Implement, integrate, test, and demonstrate the autonomous product, continuing through all safe work until a genuine authorization or external prerequisite blocks execution. This handoff does not itself authorize paid provider calls, service restarts, paper resume, testnet orders, or live trading.

The product core is autonomous futures execution, real evidence-driven learning, and strategy creation/revision:

failure history → learning → falsifiable research plan → Creator/Critic → deterministic OOS/walk-forward/stress evaluation → qualification → paper admission or explicit rejection → execution/feedback → next bounded cycle → testnet/live-readiness → separately approved first-live lifecycle.

Use concise BM/Manglish updates. Report outcomes, evidence, blockers and decisions—not each tool call. Strict TDD, minimal diffs, YAGNI. One writer; do not run competing coding agents against this checkout. Do not start a new project or rewrite working foundations.

## Verified handoff identity

- Local verification timestamp: 2026-09-15T11:05:31+08:00.
- Root: `C:\Users\thaqi\Projects\Autonomous Futures Bot`.
- Repository: `kipopopo/autonomous-futures-bot`.
- Branch: `main`.
- Source HEAD: `a15f9614f009472fde1addc65880737f146fab45` — `fix: require canonical learner arrays`.
- Working tree was clean immediately before this handoff file was written.
- Prior release readback: HEAD equalled origin/main; quality run `34920978341` reported Success. Recheck exact SHA/run before relying on release state.
- Most recent focused execution: 54 passed in 2.77s; targeted Ruff, format, mypy and compile passed.
- Historical full local Base suite: 2,247 passed; this is NOT the final current suite count.
- `pyproject.toml` requires Python >=3.14,<3.15. Use the locked uv environment, not the host's Python 3.11.

This file is a delegation artifact, not a claim of fresh VPS health or final product completion.

## First actions: discover, then implement

1. Recheck Git status/branch/remotes; preserve uncommitted changes. Read repository instructions, README, manifests, quality workflow and existing handoffs. If another writer is active, resolve ownership before editing.
2. Trace the actual daemon, scheduler, candidate loader, strategy registry, learner/trainer, Creator/Critic, order routing, ledger, API/dashboard and Telegram call paths. Identify each as implemented, connected, runtime-proven, or missing. Source presence is not integration evidence.
3. Read `verification/AUTONOMOUS_RESEARCH_BASE_V1_20260913.md` and the source entrypoints below. Reconcile old provider notes with current policy/code; never inherit provider assignments or credentials from another project.
4. Maintain one completion matrix and one evidence report rather than proliferating phase documents. Begin implementation of the critical path immediately after tracing it; do not deliver only an audit.

## Current facts and immediate defect boundary

Offline Base v1, direct/audited Google transports, typed factory, strict schemas, immutable artifacts and prepare-only provider smoke exist. Base is NOT the complete operational autonomous platform.

Four separately approved learner requests were consumed:
- First and second: transport HTTP 200, strict schema rejected.
- Third: accepted/schema_valid, typed artifact readback succeeded, but artifact and audit were only temporary and deleted.
- Fourth: HTTP 200, rejected with `recommended_novelty_dimensions:value_error`; no typed learner artifact produced.
- Latest prompt fix explicitly requires lexicographically sorted, unique arrays. It is tested offline but NOT provider-revalidated.

Do not infer the exact raw fourth-response defect from generic `value_error`; canonicality is consistent with it, not forensic proof. Raw responses were deliberately not retained.

There is no reusable accepted durable learner artifact established by those smokes. Do not fabricate one, reconstruct one from a hash, or call planner using a fixture represented as real evidence. Crucially, do not repeat the temporary-artifact deletion mistake: a capture intended for downstream handoff must retain validated typed learner artifacts and sanitized audit envelopes in an approved durable evidence store. Only disposable runner files should be cleaned up.

No unused provider request budget remains. No planner smoke has been established. No autonomous cycle or new deployment follows implicitly from HTTP success.

Historical operational snapshot only: paper runtime HALTED, zero active positions, ledger 166 opens/166 closes, candidate registry empty. Not freshly observed; do not call it current healthy state. Paper resume preparation/apply machinery exists, but actual apply was not authorized/executed. Prior strategy research was closed as economically/OOS rejected; there is no proven qualified profitable strategy to activate.

## Delivery outcomes and acceptance criteria

### A. Durable provider-to-research integration

Implement/reuse a supported finite CLI/orchestration entrypoint rather than disposable smoke scripts. It must preflight credentials without exposing values; bind run, input hashes, full failure history, policy, role/model and request budget; enforce zero retries/no fallback by default; write durable typed accepted artifacts and sanitized audit records; verify them on read; reject partial/orphan/tampered/replayed checkpoints before network; preserve rejected outcomes honestly.

Tests must cover accepted/rejected responses, schema errors, provider failure, storage failure, restart/reuse, budget exhaustion and zero-network preflight rejection. Fake transport fixtures are test evidence only. Prove all offline paths now. Real learner/planner calls require separate explicit finite approval. If approved learner succeeds, retain it for planner; do not regenerate it unnecessarily.

### B. Complete autonomous learning and strategy-creation loop

Connect actual failure and paper feedback producers to learning, planning, Creator/Critic, deterministic evaluation and immutable lineage. Reuse existing infrastructure. Distinguish LLM failure analysis from actual model training: define training objective, causal feature/label inputs, dataset cutoff, trainer invocation, model artifact/version, evaluation and adoption gates if the product claims trained-model learning. No claim of self-learning from an unused model file or prompt alone.

Use complete historical forbidden candidate IDs and thesis fingerprints. Feed a real rejected result back into a materially different hypothesis without relabeling an old candidate or recycling selection data as untouched holdout. Demonstrate restart-safe bounded cycles, call/cost/time limits, stop reasons, failure recovery, and auditable next-cycle input. No unbounded model spending or retry loops.

### C. Deterministic evaluation and admission

Enforce causal features and timestamps, immutable market-data bindings, contiguous windows, OOS/walk-forward/stress separation, realistic fees/slippage/funding when available, conservative leverage/liquidation accounting and Decimal ledger arithmetic. Missing evidence stays UNAVAILABLE, never fabricated zero.

Qualification and admission are deterministic authority boundaries outside AI control. Preserve thresholds fixed before outcomes. Rejected/no-candidate is valid; never weaken gates, force trade, tune simply to manufacture profits, or claim consistent edge from small samples. Demonstrate both qualified-fixture admission and real rejection paths, labeled separately.

### D. Paper execution and feedback closure

Connect qualified candidate loading to the actual paper runtime behind explicit admission/resume gates. Verify at most one position per pair, cash/margin/equity reconciliation, durable close synchronization, protective exits, fee/P&L attribution, restart recovery and no duplicate orders/positions. Verify feedback flows from durable actual outcomes back to learning/creation rather than analytics only.

Use isolated deterministic replay and fault injection first. Show daemon-driven entry/protection/exit/reconciliation—not merely direct engine method calls. Paper resume/restart requires separate approval; do not clear HALTED by restart or bypass the breaker. An unqualified research outcome cannot activate paper.

### E. Operational product and release

Complete bounded scheduling, idempotency/locking, health/freshness, audit/budget visibility, alerts, recovery/rollback runbooks and evidence-backed dashboard/API/Telegram. Dark accessible UI; MYT/GMT+8 display, UTC event storage. Show unavailable/stale/rejected honestly. Define measured capacity limits and backpressure for scalable use rather than overbuilding infrastructure.

Run canonical checks and exact-SHA cloud quality. Deploy only when release and operational gates allow; preserve databases, logs, caches and credential staging. No restart/service activation implicitly authorized. After approved deployment, read back deployed source identity, service state/PID/restart counts, scheduler health, cycle artifacts and admission state. A unit test or service template is not production evidence.

### F. Testnet and first-live completion boundaries

Implement and test fail-closed readiness interfaces and isolated simulation without enabling exchange execution. Fresh testnet/live evidence must bind the exact candidate, policy, account reconciliation, capital/risk scope, expiry, idempotency and kill switch. Historical evidence is not reusable blanket authority.

Before real testnet or live: obtain explicit applicable approval and verify venue/account/legal eligibility, secure credentials, capital limits, protection, kill switch and reconciliation. First-live authorization must be specifically bounded to one open → reconciliation → reduce-only close → flat reconciliation lifecycle. It does not authorize unattended live scheduling, retries, automatic promotion, or additional trades.

Do not mark the entire project operationally complete while these runtime gates remain unproven. Deliver all safe implementation first, then report exactly which approval-dependent acceptance checks remain BLOCKED. Economic profitability is not guaranteed by software completion.

## Non-negotiable safety and secret handling

- No force trade/close, breaker bypass, automatic resume, VPS restart, testnet/live activation or exchange order without explicit scope-specific approval.
- This delegation is not a fresh provider budget. Present one consolidated budget proposal with roles/models, call caps, cost/time bounds, retained artifacts and stop conditions; execute only what the user approves. Continue independent offline work while requests are blocked.
- Default execution/live/network authority remains disabled wherever not specifically authorized. Provider network authority never grants exchange authority.
- Never print/read out/stage/commit credentials, raw authenticated headers, provider raw responses, private keys, connection strings or private infrastructure details. Repository `.env` is ignored; resolve through approved application code only. Do not copy it into examples or handoffs. Never use another provider's key or home OAuth as fallback.
- Keep durable typed artifacts and safe hash/field-code audits, not raw provider content. Treat typed provider text as untrusted input.
- For authorized operational readback, SQLite `mode=ro`, `PRAGMA query_only=ON`, `busy_timeout=1000`; obtain a consistent online backup for isolated research, not a live writer DB connection.
- No lowering qualification thresholds, overwriting immutable evidence, invented trades, invented test output, or silent fallback.

## Source map (verify definitions and callers)

- `src/autonomous_futures/pipeline/autonomous_base.py`
- `src/autonomous_futures/pipeline/autonomous_cycle.py`
- `src/autonomous_futures/research/autonomy_contracts.py`
- `src/autonomous_futures/research/autonomy_prompts.py`
- `src/autonomous_futures/research/autonomy_provider.py`
- `src/autonomous_futures/research/provider_smoke.py`
- `scripts/prepare_autonomy_provider_smoke.py`
- `src/autonomous_futures/research/google_ai_studio_provider.py`
- `src/autonomous_futures/research/creator_generator.py`
- `src/autonomous_futures/research/creator_failure_feedback.py`
- `src/autonomous_futures/research/learner_training_pipeline.py`
- `src/autonomous_futures/research/qualification_artifacts.py`
- `src/autonomous_futures/research_lab/model_policy.py`
- `src/autonomous_futures/research_lab/model_audit_persistence.py`
- `src/autonomous_futures/paper/resume_control.py`
- `verification/STRATEGY_RESEARCH_FINAL_CLOSURE_20260913.md`
- `verification/PAPER_RESUME_CONTROL_20260913.md`
- `verification/PAPER_RECOVERY_PREFLIGHT_20260913.md`

Locate actual runtime/dashboard/scheduler entrypoints from the repository; their current paths are intentionally not guessed here.

## Verification and delivery workflow

Write failing regressions before production fixes; trace sibling paths. Use real locked tools from repo root:

```bash
unset PYTHONPATH PYTHONHOME VIRTUAL_ENV
uv run --locked pytest -q
uv run --locked ruff check src tests scripts
uv run --locked ruff format --check src tests scripts
uv run --locked mypy src scripts
uv lock --check
git diff --check
```

Inspect `.github/workflows/quality.yml` for additional canonical checks. Run focused tests during implementation; final full regression must bind the final source tree. Commit/push verified outcome-sized changes under the established repository workflow; verify exact-SHA GitHub Actions terminal success. If `gh` is unavailable, use supported REST/Actions evidence, not fabricated status. Never commit secrets or generated raw provider data.

Maintain a matrix with requirement, producer/consumer entrypoints, canonical tests, real evidence path/hash, runtime status and remaining gate. Final delivery must include exact commits and CI URLs, test results, end-to-end artifact lineage, release identity where deployed, recovery/runbooks and explicit remaining limitations. Distinguish IMPLEMENTED, OFFLINE-VERIFIED, PROVIDER-VERIFIED, DEPLOYED, RUNTIME-PROVEN and BLOCKED.

Start now with repository discovery and the durable-evidence integration defect. Continue safe implementation through A–F without repeatedly asking to proceed. Stop only where authority, unavailable evidence, credentials, infrastructure, or economic qualification actually prevents the next action; explain what can still be completed independently. Do not claim full completion for a partial foundation.
